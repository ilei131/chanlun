# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chanlun
DB_USER=postgres
DB_PASSWORD=your_password

# 数据获取配置
# 请求间隔(秒)，避免请求过快被封
REQUEST_DELAY=0.5
# 批量插入大小
BATCH_SIZE=1000
# 最大重试次数
MAX_RETRY=3
# 并发数
CONCURRENT_REQUESTS=5

# 数据更新配置
# 是否自动更新
AUTO_UPDATE=false
# 每日更新时间
UPDATE_TIME=18:00
